# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/
mercadoum-firebase-adminsdk-fbsvc-6c500851ae.json
firebase-service-account.json

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Release signing (never commit these)
/android/key.properties
/android/*.jks
/android/*.keystore
nombre-del-archivo.json
.superpowers/

# Variables de entorno (credenciales SMTP/Twilio, JWT_SECRET). Nunca se
# commitean — ver backend/.env.example para la lista de variables.
.env
backend/.env

# SQLite database (contiene datos de producción/dev locales — nunca debe
# viajar por git: un deploy que la sobrescriba corrompe el WAL en vivo del
# servidor, además de filtrar datos de usuarios).
*.db
*.db-shm
*.db-wal
